{
  "name": "allow-copy-plus-origin",
  "version": "3.0.11",
  "description": "A privacy-first, ultra-lightweight fork of the Allow Copy + extension rewritten in TypeScript.",
  "main": "dist/background.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node test/hardening.test.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
